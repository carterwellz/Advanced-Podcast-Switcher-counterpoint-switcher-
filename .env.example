# Copy to .env and fill in. .env itself is gitignored.
#
# AssemblyAI API key, used only by filler-word trim (trimFillerWords). Nothing else
# in the plugin makes a network call, and with that toggle off this is never read.
CPSW_ASSEMBLYAI_KEY=
