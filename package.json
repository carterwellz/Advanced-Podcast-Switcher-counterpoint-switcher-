{
  "name": "counterpoint-switcher",
  "version": "0.1.0",
  "private": true,
  "description": "Audio-driven multicam switcher with reaction shots for Premiere Pro, built for Counterpoint Studios",
  "type": "module",
  "scripts": {
    "analyze": "tsx src/cli/analyze.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
